# 可选知识图谱迁移包

此目录保存已经完成处理、可供用户自行体验的 `.kmn.zip` 文件：

- `三国志.kmn.zip`
- `改命记实录(道之光).kmn.zip`

这些文件不会在后端启动时自动导入。需要体验时，在软件上传页点击上传区或直接拖入其中一个 `.kmn.zip` 文件；导入过程不调用 AI。若系统中已经存在同名文件或同名图谱，请先确认并处理已有数据，否则导入会被拒绝。

后端默认自动导入的仅为 `backend/default_examples/本软件使用说明.kmn.zip`。
